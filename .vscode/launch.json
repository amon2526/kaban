{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CMake Build",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/kaban.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "setupCommands": [
                { "text": "-enable-pretty-printing", "ignoreFailures": true }
            ]
        }
    ]
}
