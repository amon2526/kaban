{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build debug",
            "type": "shell",
            "command": "cmake --build --preset debug",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "build release",
            "type": "shell",
            "command": "cmake --build --preset release"
        },
        {
            "label": "build debug test",
            "type": "shell",
            "command": "cmake --build --preset debug"
        },
        {
            "label": "build debug test",
            "type": "shell",
            "command": "cmake --build --preset release"
        }
    ]
}

